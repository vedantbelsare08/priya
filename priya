#include<iostream>
#include<conio.h>
using namespace std;
int main()
{
int n = 5;
int a[n]={8,9,18,21,30},First=0,last=n-1,mid,num;
num=21;
do{
mid=(frist+last)/2;
if(a[mid]==num)
{
cout<<"found at this locatin<<mid+1;
break;
}

else if(a[mid]<num)
first = mid+1;
else
last=mid-1;
}
while(first<=last)
if(first>last)
cout<<"element not found";
return 0;
}
